# Supabase configuration.
# Copy this file to `.env.local` and fill in your project values.
# The app runs in offline (localStorage) mode when these are not set.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
